# 戦闘画面（配置設計）

このファイルは配置（レイアウト）の設計をまとめたもの。**この配置を使った戦闘の本実装（100ステージ・10連戦）は[battle-test.md](./battle-test.md)（`/games/game1/battle`）を参照**。実データは [lib/character-growth.ts](../../lib/character-growth.ts)（仲間のレベル成長）、[lib/enemy-scaling.ts](../../lib/enemy-scaling.ts)（敵の出現パターン・強さ）、[lib/characters-info.ts](../../lib/characters-info.ts)（仲間の基本情報）、[lib/enemies-info.ts](../../lib/enemies-info.ts)（敵の基本情報）を参照。

## 画面構成

戦闘背景（[fields.md](../fields.md)）の上に、左側に仲間パーティ、右側に敵を左右対称に配置する。

- 仲間・敵とも最大3体まで、縦一列に配置
- 1体目は画面の上下中央あたり、2体目・3体目はそこから上に向けて、重ならない範囲でできるだけ詰めて配置する
- 仲間は左側、敵は右側で、位置は左右対称（同じ縦位置、水平方向だけ逆）
- キャラクター表示サイズは、額縁画面の[character-view.md](./character-view.md)などとは別に、戦闘画面独自のサイズを使う。当初は幅16%だったが、背景を地面拡張版に差し替えてスペースが広がったのに合わせて幅24%（1.5倍）に拡大した（現在の値・スロット位置は[battle-test.md](./battle-test.md#配置サイズslot_positions)参照）

## 仲間パーティの人数

- パーティは1〜3体編成。2体・1体でも組める
- 人数が3体未満でも、配置スロットの位置（1体目・2体目・3体目の座標）は変えない。編成した人数分だけ、スロットを上から順に埋める（3体編成なら1〜3体目全部、1体編成なら1体目のスロットだけ使う）

## 敵の配置

- どのステージでどの敵を何体出すかは、ステージ番号から計算で決まる（[lib/enemy-scaling.ts](../../lib/enemy-scaling.ts)、詳細は[adventure-system.md](../adventure-system.md#敵の出現パターン強さ背景)参照）。通常バトル（`S-1`〜`S-9`）は3体、ボス戦（`S-10`）は1体（中央スロットに配置、他は空）
- 敵側は仲間と違い「何体編成するか」をプレイヤーが選ぶものではなく、ステージ側で自動的に決まる

## 検討中のモックアップ

Artifactで作成した配置サンプル（[戦闘画面 仲間＋敵配置案](https://claude.ai/code/artifact/e54b820d-608f-40ea-95e4-0b37e7b6e736)）で、仲間3体の縦位置と、敵3体を左右対称に配置した見た目を検証済み。画像キャンバス自体に上下の透明な余白があるため、キャンバスの高さではなく実際に見えている絵の範囲を基準に間隔を詰めている（詳細は[characters.md](../characters.md)の透過処理の説明も参照）。仲間・敵とも同じ縦位置・同じサイズ（幅16%）を使い、水平方向だけ反転させることで左右対称にしている。

## 今後

- 上記モックアップの位置・サイズを土台に本実装済み（[battle-test.md](./battle-test.md#配置サイズslot_positions)参照）。その後、背景の差し替え・サイズ拡大に伴って位置・サイズ自体は調整されている。ターン進行・ダメージ計算・100ステージ分の敵生成も本実装済み（[adventure-system.md](../adventure-system.md)参照）
- 「冒険」を戦闘画面だけで構成するか、戦闘外の探索フィールドを別途用意するかは未定（[fields.md](../fields.md)参照）
