# アイテム素材

## 保存場所・命名規則

実際にアプリで使う画像は [public/items/](../../public/items/) に置く。

ファイル名: `i{連番3桁}.png`（`i`はitemのi）。実データは [lib/items-info.ts](../../lib/items-info.ts) を参照。

現在 `i001`〜`i250` の250個分が揃っている。内訳は以下の4種類（`type`）。

| type | 対応する武器種 | 個数 | ID範囲 |
| --- | --- | --- | --- |
| 片手剣 | 仲間の武器種「片手剣」用（アカネ） | 25 | i001〜i025 |
| 法器 | 仲間の武器種「法器」用（カエデ・コユキ） | 25 | i026〜i050 |
| 弓 | 仲間の武器種「弓」用（サユミ） | 25 | i051〜i075 |
| アーティファクト | 武器種を問わない装飾品 | 175 | i076〜i250 |

武器種の対応は[roster.md](./roster.md)の仲間キャラクター一覧（`weaponType`）を参照。

### アーティファクトの内訳（`slot`）

アーティファクト175個は、装備部位ごとに7種類×各25個（レアリティ5種×5個）に分かれている。

| slot | 個数 | ID範囲 |
| --- | --- | --- |
| 護符 | 25 | i076〜i100 |
| 指輪 | 25 | i101〜i125 |
| 腕輪 | 25 | i126〜i150 |
| 首飾り | 25 | i151〜i175 |
| 耳飾り | 25 | i176〜i200 |
| ブローチ | 25 | i201〜i225 |
| 帯留め | 25 | i226〜i250 |

### レアリティ

`C` / `B` / `A` / `S` / `SS` の5段階。武器種・アーティファクトのslotごとに、各レアリティ5個ずつ揃っている（レアリティが上がるほど強い、という前提だが数値バランスは未定）。

## 元データ・画像の作り方

アイテム画像は`WebApp_game`とは別の場所（`C:\Users\田中宏樹\Documents\Codex\hirogames_images\assets\items\catalog_v1`）でユーザー側が用意したもの。この元フォルダも[characters.md](./characters.md)の`サンプル画像/`と同様、**中身を消したり移動したりせず、`public/items/`へコピーして使う**。

元画像は1254×1254px・1枚あたり約1.2〜1.5MBで、レアリティごとの背景色・枠・レアリティ文字が画像に焼き込まれた正方形アイコン（透過なし）。250枚をそのままのサイズでコピーすると容量が大きくなりすぎるため（合計300MB超）、`sharp`でニアレストネイバー法（`kernel: "nearest"`）を使い256×256pxへ縮小してから`public/items/`に配置している。ドット絵特有の輪郭のカクカクした感じを保ったまま、合計約29MBまで縮小できることを確認済み。

## データ構造（基本情報）

[lib/items-info.ts](../../lib/items-info.ts)の`ItemBaseInfo`が、アイテム1個につき一意に定まる基本情報（ID・名称・説明文・種類・レアリティ・画像パス、アーティファクトのみslot）を持つ。

## 装備によるステータス加算

実装: [lib/item-effects.ts](../../lib/item-effects.ts)。装備1個につき、種類（武器種／アーティファクトのslot）ごとに決まった1つのステータスへ効果を発揮する。

| 装備 | 効果ステータス |
| --- | --- |
| 武器（片手剣／法器／弓） | 攻撃力（**固定値**加算） |
| 護符 | HP（％加算） |
| 指輪 | 攻撃力（％加算） |
| 腕輪 | 防御力（％加算） |
| 首飾り | 会心率（ptを直接加算） |
| 耳飾り | 会心ダメージ（ptを直接加算） |
| ブローチ | 属性耐性（ptを直接加算。属性システム自体が未実装のため、当面は数値の表示のみで戦闘には影響しない） |
| 帯留め | HP／攻撃力／防御力の3つに、それぞれ半分の割合で少しずつ（％加算） |

**武器だけ「攻撃力への固定値加算」、それ以外（アーティファクト）は「％加算」**（ゲームデザイン上の好みとして、武器はレベルアップとは別に足し算で分かりやすく伸ばしたい、という方針。ユーザー確認済み）。

上昇量はレアリティ別（ユーザー確認済みの仮数値、後から係数を書き換えて調整可能）。

- 武器の固定値：レア度ごとに5段階のレンジ（下表）を持ち、**同じレア度でも武器5種類ごとに値が異なる**（ユーザー確認済み：レンジ内で均等にばらつきを持たせる）。中心値はキャラのレベルアップ1回あたりの伸び幅（例えばアカネなら攻撃力+6.0/レベル）と比べて、装備を替えたことがはっきり実感できる大きさを狙っている
  - C: 26 / 28 / 30 / 32 / 34
  - B: 51 / 55 / 60 / 65 / 69
  - A: 94 / 102 / 110 / 118 / 126
  - S: 153 / 166 / 180 / 194 / 207
  - SS: 255 / 277 / 300 / 322 / 345
  - どの値がどの武器（アイテムID）に対応するかは、同じ種類・同じレア度の5個をID昇順に並べた位置（0〜4番目）で決める（[lib/item-effects.ts](../../lib/item-effects.ts)の`weaponFlatAtkForItem()`）。ハードコードではなく、`items-info.ts`側の並び順から自動的に決まる
- HP・攻撃力・防御力に効くアーティファクト：そのステータスの基礎値（レベルなりの値）に対する割合加算。C+3% / B+6% / A+10% / S+16% / SS+25%（帯留めはこの半分をHP/攻撃力/防御力それぞれに）
- 会心率・会心ダメージ・属性耐性に効く装備：%ポイントを直接加算。C+2pt / B+4pt / A+7pt / S+11pt / SS+16pt

武器1つ＋アーティファクト3枠、装備している分すべてを合算する（[character-view.md](./screens/character-view.md)の装備欄・[lib/game1-data.ts](../../lib/game1-data.ts)の`CharacterEquipment`参照）。キャラクター画面のSTATUS表示・戦闘（[battle-test.md](./screens/battle-test.md)）の両方に反映される。

**装備は所持数分しか使えない**（前提事項、調整対象ではない）。同じ個体（インスタンス）を複数キャラクターへ同時装備することはできない。武器・アーティファクトを問わず、キャラクター全員の装備欄（武器1枠＋アーティファクト3枠×人数）に使われている個体は、他のスロットの候補一覧から除外される（[lib/game1-data.ts](../../lib/game1-data.ts)の`isInstanceEquippedElsewhere()`）。1つしか持っていないアイテムは1人にしか装備できないため、パーティ全員を同じ強さの装備で揃えるには複数個ドロップさせる（または合成で個体を増やすのではなく強化する）必要がある。

## アイテムの個体管理（合成のための前提）

アイテムは「ID＋所持数」ではなく、**1個1個が固有の個体（インスタンス）**として管理される（[lib/game1-data.ts](../../lib/game1-data.ts)の`ItemInstance`：`instanceId`・`itemId`・`plus`）。同じ`itemId`のアイテムでも、下記の合成によって個体ごとに強化度合い（`plus`）が異なりうるため。装備欄（`CharacterEquipment`）が参照する値も、アイテムIDではなく個体ID。

所持数の上限は**100個体**（[lib/game1-data.ts](../../lib/game1-data.ts)の`INVENTORY_CAP`）。上限に達している状態でアイテムがドロップした場合は受け取れない（「受け取れませんでした」とだけ表示し、**何のアイテムだったかは表示しない**——レアなアイテムだった場合に悔しい思いをさせないための仕様）。

## アイテムの合成（強化）

実装: [lib/item-synthesis.ts](../../lib/item-synthesis.ts)。ユーザー確認済みのルール：

- 対象：**同じ武器種同士**（片手剣／法器／弓、種類が同じならレア度・見た目違いでもOK）、または**同じアーティファクトslot同士**（護符同士、指輪同士…）のみ。武器種を跨いだり、slotが違うアーティファクト同士は合成できない
- 一括合成対応：残す方（対象）を1つ選び、素材を複数まとめて選んで1回で合成する
- 合成後に残る個体は、関わった中で**一番レア度が高いもの**（見た目・アイテムIDはそのまま）。**同レア度どうしの場合は、プレイヤーが「対象」として選んだ方**が残る（レア度と見た目・IDの対応関係自体は常に固定で、合成によって変わることはない）
- 素材1個につき、（その素材自身の`plus`値 + 1）に「レア度差の割引」を掛けた分だけ、残る個体の`plus`に加算する
  - レア度差の割引：1段差=1/2、2段差=1/4、3段差=1/8、4段差=1/16（2^段差で割る）。同レア度は割引なし（1倍）
  - 「+1」しているのは、真新しい（`plus`が0の）素材でも1回分の合成価値（後述の5%）は必ず持つようにするため
- `plus`は内部では端数（小数）まで正確に保持し、**実際の効果・表示には切り捨てた整数値**を使う。切り捨てた結果+0のままになる組み合わせ（例：低レアを大量に上位レアへ合成しようとした場合）もあるため、合成前にプレビュー（変化後の値）を必ず表示し、確認してから確定する
- 効果量は`plus`1につき、**そのアイテム自身の元の基礎値の5%**増加（複利ではなく毎回同じ幅）。武器（固定値）ならその5%、アーティファクト（％／pt）ならその数値自体の5%
- 合成はキャラクター画面から行う。素材・対象にできるのは「未装備のアイテム」と「今表示中のキャラクター自身が装備しているアイテム」のみ。**他のキャラクターが装備中のアイテムは候補に出ない**（見えていないキャラクターの装備が知らないうちに消費されるのを防ぐため）。消費された個体がどこかに装備されていた場合は、装備欄から自動的に外れる

## アイテムごとの能力差

同じ種類・同じレア度の5個でも、以下の2つの仕組みで個体ごとに差が出るようになっている。

- **武器の固定値ばらつき**：上記「装備によるステータス加算」の通り、ID順の位置で固定値が5段階に分かれる（見た目・アイテムIDに紐づく差。ドロップのたびに変わるわけではない）
- **武器のランダム個体差（追加能力枠）**：武器（アーティファクトを除く）がドロップした瞬間に、能力の種類・数値がランダムに1つ決まり、**その個体が存在する限りずっと固定**される（合成の＋値強化の対象外）。実装：[lib/item-substat.ts](../../lib/item-substat.ts)の`rollWeaponSubstat()`、[lib/game1-data.ts](../../lib/game1-data.ts)の`ItemInstance.substatStat`/`substatValue`
  - 候補となる能力は、アーティファクトの効果と同じ6種：HP／攻撃力／防御力／会心率／会心ダメージ／属性耐性
  - レア度ごとの%レンジ（ユーザー確認済みの仮数値。会心率・会心ダメージ・属性耐性が選ばれた場合は、ロールした数値をそのままpt（%ポイント）として使う）
    - C: 1.0〜2.0% / B: 1.5〜3.0% / A: 2.5〜4.5% / S: 4.0〜7.0% / SS: 6.5〜10.0%
  - ステータス計算への反映は[lib/item-effects.ts](../../lib/item-effects.ts)の`calculateEquipmentBonus()`で、通常の（レア度＋ばらつきによる）武器効果に上乗せする形で加算する。合成の＋値による倍率（`synthesisMultiplier`）の対象外

アーティファクトには追加能力枠は付かない（対象は武器のみ）。

## アイテムドロップ

実装: [lib/item-drop.ts](../../lib/item-drop.ts)。ドロップ確率・レアリティ抽選テーブルは[adventure-system.md](./adventure-system.md#アイテムドロップ)参照。レアリティが決まった後、どのアイテムになるかはそのレアリティの全アイテムから均等ランダム（武器種問わず）。

## アイテム効果の確認（詳細ポップアップ）

実装: [lib/item-effects.ts](../../lib/item-effects.ts)の`describeItemEffect(instance, item)`。個体（`ItemInstance`）とその基本情報から、実際に効いているステータスを「ステータス名：+数値」の一覧（`ItemEffectLine[]`）に変換する。中身は`calculateEquipmentBonus()`をそのまま再利用しており（武器ならweaponスロット、アーティファクトなら1枠に仮装備させた状態を作って渡す）、レア度・武器のばらつき・合成の＋値・武器のランダム追加能力（あれば）を含めた「装備した場合に実際どうなるか」と常に一致する。0の項目は表示しない。

個体情報を持たない集計後の表示（戦闘結果のドロップ一覧など）では、`plus:0`・追加能力なしの仮インスタンス（`{instanceId:"preview", itemId, plus:0}`）を渡すことで、レア度なりの基礎効果だけを取り出せる。

この効果一覧は、以下の画面で見た目を統一したポップアップ（不透明の`#241f47`カード、UI全体で使っている枠と共通のスタイル）として表示される。

- [戦闘結果画面](./screens/battle-test.md)：獲得アイテムのアイコンをタップした時
- [仲間（キャラクター）画面](./screens/character-view.md)：装備を切り替えた直後の確認ポップアップ

## 画面での利用

所持しているアイテムを一覧表示する[バッグの中身画面](./screens/bag.md)（`/games/game1/bag`）で使用している。所持数の管理方法は[save-data.md](./save-data.md)の`Game1SaveData.inventory`を参照。

## 今後（未定・空欄のパラメータ）

- 入手方法のうちショップ販売（現状はドロップのみ）
- 価格（購入価格・売却価格）
- アーティファクトの装備数上限やslotごとの同時装備制限（現状は3枠ともどのslotのアーティファクトでも装備可能。片手剣/法器/弓が対応する武器種の仲間しか装備できない点、所持数分しか使えない点は確定済み）
- 属性相性表が決まったら、ブローチの属性耐性を実際のダメージ計算に反映する
